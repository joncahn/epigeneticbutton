# profiles/slurm/config.yaml
executor: slurm
jobs: 16
use-conda: true
conda-frontend: conda
latency-wait: 60
restart-times: 1

sbatch:
  cpus-per-task: "{threads}"
  mem: "{resources.mem_mb}"
  qos: "{resources.qos}"
  slurm_extra: "--job-name={rule}_{jobid}"