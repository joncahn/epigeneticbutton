name: epibutton_ont
channels:
  - conda-forge
  - bioconda
dependencies:
  # Note: modkit is installed via binary download in the workflow
  # (not available on conda, get from https://github.com/nanoporetech/modkit/releases)
  - minimap2>=2.28
  - samtools>=1.17
  - bedtools>=2.31
  - ucsc-bedgraphtobigwig
  - htslib>=1.17
  - pigz
  - python>=3.10
  - requests  # for downloading modkit binary
